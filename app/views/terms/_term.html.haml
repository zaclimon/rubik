.col-sm-6.col-xs-12
  .panel.panel-default
    .panel-heading
      .panel-title= term_title(term)

    .list-group
      - term.academic_degrees.each do |academic_degree|
        = link_to academic_degree.name, new_term_agenda_path(term, academic_degree_id: academic_degree.id),
          class: "list-group-item"

        -#= link_to academic_degree_term.name, new_academic_degree_term_agenda_path(academic_degree_term),
        -#  class: "list-group-item"
